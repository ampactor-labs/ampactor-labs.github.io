{"mentl/5e780c0":"ty_to_wasm_type — the composition repr_of |> repr_wat with no consumer,\nwhile repr_wat itself answers 19 references, so callers reach the\nprojection directly.\n\nhandle_recorded — a membership walk whose one reference is its own\nrecursive call. The facet counts references outside the declaration's\nextent, which is the distinction that makes a self-recursive orphan\nvisible where a raw ref-count reads 1 and looks alive.\n\nemit_load_chain — a one-line wrapper defaulting leaf_repr to RI32, while\nall four callers use emit_load_chain_repr. Its comment moved onto the\nsurviving fn.\n\nThe emit line count did not move: 418641 before and after. That is the\nindependent confirmation that all three were unreachable — reachability\nhad already pruned them, so this is source hygiene and not a behaviour\nchange. A deletion that moved the artifact would have meant the\nmeasurement was wrong.\n\nThe facet's over-report is what its entry says, measured rather than\nassumed. The remaining backends/wasm rows are type names, and\ncursor_transport's three — Surface, Action, PatchWrite — are effect names\ncarried in cursor_step's declared row, where type position draws no\nreference edge. Two probes settled it.\n\nThe comment-ref ratchet then caught prose in types.mn still backticking\nty_to_wasm_type; rewritten to name repr_wat, which is what those emit\nsites actually read.\n\nBoard: m2 == m3 at 418641 lines, census 0; micros 149/149; frontier 374/0;\ndoc-truth green. boot re-pinned 40b1c714.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_01XKLpV6mmz2vuGoeLjQju66","mentl/5e94f33":"Both of 5.1's measured legs ran in one iteration. The cycle-guard leg\ndied twice: the mangle space per base is finite by construction (a\nwide component is a scalar repr and containers are words), and\npolymorphic recursion cannot reach the emit at all — the signature'd\nprobe refuses E_OccursCheck, because the checker uses the mono\nassumption for self-calls even under a signature. That last fact is a\nmeasured 5.3 baseline: Mentl today refuses polymorphic recursion both\nways, below the crude annotate-or-refuse route.\n\nThe worthiness-deletion leg was built whole — nine fns gone, demands\nequal candidates — and the march refuted it: m2 compiled and m3\ntrapped, call stack exhausted in zip_with. The gate is load-bearing\ncorrectness, not a perf hybrid: the worthy set was leaf-compute fns by\nconstruction, so the twin emission for self-recursive closure-carrying\nHOFs was never exercised, and it miscompiles. Reverted whole per the\nbroken-verdict law; the real blocker is the new peer\nHβ.emit.plumbing-twin-selfcall with its named next probe (force one\nplumbing twin, emit-diff against the floor, pin the break).","mentl/5ec2b78":"The Pure predicate-fn unfold was built whole (332-line patch banked:\nthe binder env absorbing the self special-case, the bool/match\nevaluator, the body lookup through the env reason + the span log, the\nPure-row gate, the one-level PBoolNode hook) and its row widens\nflipped main's row to a phantom Intern no spine component performs —\nthe parts-don't-sum signature's third appearance. Row-perturbing\nengine work in verify/infer gambles the attractor every time; the\nmovers root outranks every queued increment until fixed. Tree\nrestored to pin fb265daa; board green.","mentl/5ef782d":"Every path-taking verb shared one silent seam, measured on the audit:\na path outside the standing mounts (cwd, /tmp, the repo at /mentl-home)\nhad no guest route, fs_read_file failed, and driver_check_entry parsed\nthe emptiness — zero fns at exit 0, every analysis verb lying by\nomission. Both layers fixed at their owners: the shim (the mount seam's\nowner) derives one more preopen from the first path-shaped argument,\naddress forms included, so every verb reaches the file the caller\nnamed; and driver_check_entry mirrors the weave fold's own guard —\nan unreadable entry reports EMissingModule and never parses emptiness.\n\nMARCH_REPIN blessed the pin (CLEAN m2 == m3 at 337,808 lines, sha\n0513aca1...), so the whole day's arc — the verb-shape tier, the\nfirst-first show_list, the chased-row severance, the entry refusal —\nis now what the daily mentl serves. Probed through the installed shim\nfrom outside the repo: declaration-order rows, honest severance, the\nverb-shape invite, and a loud E_MissingModule on an unreachable path.\n\nNext arc, per Morgan: daily includes the multithreaded multi-cursor\nmulti-shot behaviors — the field projection (mentl file:0) is still\nsequential, and its >< swap was gated on the runner migration, a dep\nthat died this week. The fan is unblocked residue.","mentl/5e68374":"render_at surfaces the CursorView's Propose facet it had been\ndestructuring and discarding: an L:C address at an authored ?? now\nprojects the one proven survivor (Query: ?? : Positive / Propose: 1),\nrendered through the same render_candidate_source projection the edit\ntransport's accept path applies; two or more survivors print the count\nand the teaching line, never a hidden first-wins pick.\n\nTwo structural fixes made the facet reachable. The resolver's column\nlaw: address_better_a picked the widest containing node for both\naddress forms while its own comment promised the column form narrows —\nthe code catches up (file:LINE keeps the line's root; file:L:C picks\nthe tightest containing node), and identical spans pick the latest\nmint, the more derived node at the position (a ?? mints its id cell\nthen the NHole over the same span; the address must reach the NHole).\nAnd the sharper wheel caught this session's own first attempt at a\nty_of_kind NHole arm as ill-sorted (NodeKind vs NodeBody, census\n0 -> 1 on the m3 leg) — deleted, the arm was dead code: NHole is a\nbody constructor and never appears as a chased cell state.\n\nGate: tests/frontier/propose-demo + the cursor-address-propose leg,\nseen RED on the pre-arm boot. Pin 17d1c3be, m2 == m3, census 0,\nfrontier 193/0.","mentl/5ef2d98":"Where-we-are gains the 2026-06-11 landing (one-precedence ~>, structural ==,\ninterpolation canonical with seed substrate). New section 'Standing on the\nfrontier — honestly': per-adjacent verdicts (Rust/Koka/Flix/Effekt/OCaml5/\nUnison/Zig/Hazel) grounded in the research sweep, naming the two genuinely-\nahead pieces (gradient narration; graph+Reason proof-carrying transformation).\ndocs/PLAN.md compatibility shim deleted per delete-dont-explain-absence —\nROADMAP.md is the live record; git history preserves the old path.","mentl/5e35fab":"Each test in the test list is independent (assertion counts compose\nvia fold-sum; no order dependencies). parallel_map dispatches the N\ntests to N OS threads via parallel_compose; per-test (passed, failed)\ntuples returned as a list; fold-sum merges the counts.\n\nrun_suite (verbose per-assertion output) retained sequential —\nhuman-readable output ordering is load-bearing for the verbose form;\nparallel execution there is peer follow-up Hβ.test.suite-parallel-\nwith-buffered-output.\n\nDrift-audit clean.","mentl/5e7d6a9":"Five CLI entry-point handler chains missing parallel_compose +\nwasi_threads added: audit_run, query_run, teach_run, doc_run, serve_run.\nedit_run, compile_run, compile_stdin_run, check_run already had them.\nrun_run + test_run delegate to compile_run + check_run respectively\n(transitively covered). repl_run is a stub.\n\nWithout these handlers in the chain, wheel-side parallel_map sites\nfall through to E_HandlerUninstallable or sequential degrade. With\nthem installed, every CLI verb's compute can dispatch to OS threads\nwhen parallel_compose recognizes a spawn/join effect from the\nwheel-side parallel_map calls.\n\nAudit complete — every Mentl invocation surface now ships the\nparallel handler chain.\n\nDrift-audit clean.","mentl/5ec6784":"TResume keyword (kind 10) was unhandled in parse_expr's primary\ndispatch — `resume()` parsed as bare CallExpr to ident \"resume\", which\nenv_lookup failed → LCall(LConst(0), []) → emit produced bogus\ncall_indirect from address 0 → trapped at table[0]'s wat_stdout\nsignature ($ft2 vs caller's $ft1). Empirically: every handler arm\nbody using `resume()` produced this trap shape.\n\nSubstrate move:\n- parser_expr.wat: TResume (kind 10) dispatch arm to $parse_resume_expr.\n- parser_compound.wat: $parse_resume_expr — parses `resume()` (val =\n  LitUnit), `resume(value)` (val = parsed expr); state-updates list\n  stays empty per Lock #6 (matches wheel src/parser.mn:1208 canonical\n  `ResumeExpr(val, [])`).\n- parser_infra.wat: $mk_ResumeExpr — tag 95 record [val_node,\n  state_updates_ptr] matching the layout $lower_resume reads (offset\n  4 = val_node).\n\nThe trailing `with field = expr [, ...]` clause is absorbed by the\nexisting $skip_to_arm_terminator at handler-arm boundary\n(parser_handler.wat:255). State-mutation lowering is named follow-up\nHβ.lower.resume-state-updates-threading per the existing comment in\n$lower_resume — at Tier 1 monomorphic, it's a sequence of LStateSet\nops before the LReturn (the slot index is the state-field's position\nin the handler's record).\n\nEmpirical: handler arm body `bar() => resume()` previously produced\n`(i32.const 0)(local.set $call_3)(local.get $call_3)(local.get\n$call_3)(i32.load offset=0)(return_call_indirect (type $ft1))`. Now\nproduces `(i32.const 0)(return)` — clean LReturn. mentl2 advances\nthrough compile_stdin_run → compile_stdin → frontend → parse_program\n→ parse_fn → parse_one_param → fresh_ph → graph_fresh_ty (next gate).\n\nEight interrogations: TResume IS the resume primitive — keyword\nprojection from the graph's continuation primitive (Graph), Tier 1\ntail-resumptive arm lowers to LReturn (Handler), `~>` chains routes\nthe stack but resume is the arm's continuation projection (Verb),\nno row change (Row), structural ownership (Ownership), no","mentl/5e0b7b5":"THE L1 BLOCKER. Bisected to: parser_expr.wat:107 field-access path.\n\nPre-fix path:\n  $ident_at_p(pos+1) returns NULL on keyword tokens (THandle, TMatch,\n    TWith, etc.) because kind != 25 (TIdent).\n  Stage 2B's recovery: produce NErrorExpr + advance pos by 1 (past\n    TDot only). Keyword remained unconsumed.\n  Outer parser saw THandle and started a `handle EXPR with HANDLER`\n    form, absorbing all subsequent siblings as the handle-expr's body.\n  Result: src/voice.mn's `s.handle` (legitimate field access) → 344\n  fns absorbed into voice_queue_walk's body across full src+lib.\n\nSubstrate-honest fix: SYNTAX.md §572 admits any identifier-OR-keyword\ntoken at field-access position. Mentl uses keyword-named fields like\n`s.handle`, `m.match`, `e.with`, `r.ref` throughout the wheel.\n\nUNIFICATION (per \"don't throw away graph info; utilize the graph;\nempower the graph; unify the graph\"):\n- $tokenkind_name (lex_main.wat:222) ALREADY held the kind→string\n  mapping for diagnostics. Pre-fix, it was incomplete (only 4\n  keyword cases). Now extended to cover all 25 keywords (kinds 0-24)\n  with canonical lexer_data.wat string offsets.\n- $ident_or_keyword_at_p (parser_infra.wat — NEW) delegates to\n  $tokenkind_name. Single source of truth for kind→text in the seed.\n  Drift 7 (parallel-arrays) refused: keyword table lives in ONE\n  place, not duplicated across parser/infer/emit.\n- parser_expr.wat field-access: replaced $ident_at_p call with\n  $ident_or_keyword_at_p; updated null-recovery to advance past BOTH\n  TDot AND non-ident token (advance by 2) so outer parser doesn't\n  re-parse the non-ident as a new statement.\n\nEmpirical (cat src+lib | seed):\n| Metric | Pre-fix | Post-fix | Delta |\n|---|---|---|---|\n| Output size | 4.07 MB | 3.80 MB | -270 KB |\n| Total fns | 1539 | 1572 | +33 |\n| vqw-prefixed | 344 | 0 | -344 (CLOSED) |\n| str_concat fn def | 0 | 1 | +1 (now emits!) |\n| wat2wasm errors | 14,169 | 4,305 | -9,864 (3.3× reduction) |\n\nEight-interrogation alignment per edit site:\n  Gr","mentl/5e9dedc":"Σ-/// doctrine applied (commit 940a626):\n- 1 decoration consolidated to /// lede (Pattern B): Construction → make_tuple\n- 1 section-span stripped to plain // (Pattern C): Access\n- 3 standalone // paragraphs promoted to /// on following decl:\n  make_tuple, tuple_set, tuple_get\n\nNo semantic changes; substrate ready for DS surfacing /// to Mentl.","mentl/5e91b95":"keyword_type's inner function lookup collides with the Infer effect's lookup op. The lowering prefixes effect ops to __ev_op_lookup but is_global('lookup') returns true (bare name in globals list), causing the inner function to be lowered as a global with bare name in the fn_table. Renamed to kw_find — no collision, no architectural changes needed.","mentl/5ed65b6":"VM builtins: max, min (polymorphic: Int/Float/String),\nclamp (safe: no panic on lo>hi), round, atan2, pi.\nChecker: polymorphic min/max/clamp, round, atan2, pi.\nDSP: uses abs() instead of manual if/else.\nAll golden tests pass.","sonido/5e03ad3":"- adapter.rs: extract track_peaks(&mut (f32,f32), abs_l, abs_r) helper, replacing 8\n  identical 4-line abs/max blocks across process / process_stereo / process_block /\n  process_block_stereo. Net -54 LOC, identical codegen (helper is #[inline]).\n- dag_rebuild_stress.rs: host-side regression net for the Daisy DAG-rebuild crash class.\n  5 tests using a tracking GlobalAlloc to catch SDRAM-OOM-style accumulation. Mirrors\n  the firmware's rebuild_in_place flow exactly. Disabling the harvest call inside the\n  storm loop reproduces the historical bug (caught 67 MB of accumulation across 200\n  rebuilds in verification).\n- README: 1,369 -> 1,800+ tests (actual is 1,812); sonido-config no_std column\n  Partial -> No (serde_json/toml/dirs are not feature-gated).","sonido/5eb861d":"- Remove unused adaptive_mode field from AudioProcessor struct (audio_processor.rs:113-114, 534)\n- Inline unnecessary let binding in SonidoApp::init_buffer_size (app.rs:254-255)\n\nFixes clippy warnings that were blocking stop hook.","sonido/5eb0919":"CLI_TESTING.md expands the old manual testing doc with output param\ntests, waveshape modes, foldback_threshold, stereo_spread, tremolo\nwaveforms, wah manual mode, synthesis pipelines, and analysis extras.\nUses `sonido info` instead of soxi for smoke tests.\n\nLIBRARY_TESTING.md is a new guide for library consumers covering\nno_std compilation, integration patterns, behavioral guarantees\n(golden regression, extreme params), performance baselines, and a\nper-effect gain staging reference table.\n\nRemoved test_audio/sine_440.wav — CLI generates test signals on the fly.","two-top/5e217a0":"The repository status paragraph pinned sim::SIM_VERSION at 13 and the\nsuite at 513 tests; the bank-shot change moved the version to 14 and\nthe suite now runs 527.","two-top/5ed5df0":"Replaces the per-wall dark-rectangle placeholders with a single\ntraining_floor.png backdrop. The source asset already integrates\nthe bone-tile wall pattern + checker-stone floor + spawn marks +\ncenter duel diamond as one Bone-Cathedral composition; spawning\nextra dark rectangles on top of it would just clip the polished art.\n\nWalls remain sim-only entities (no Sprite + Transform on the spawn\nbundle) — the wall_collision system reads Wall.rect for physics,\nthe visual is the backdrop. Spawn order preserved so entity ids\nstay deterministic across hosts.\n\nBackdrop sized 1100x1600 cm (slightly larger than the 1000x1500 cm\nplayable interior so the wall pattern reaches past the playfield\nedge). Z=-1.0 puts it below players, stains, and effect sprites.\n\nVerified: 261-test workspace suite passes; clippy clean.\n\nCycle 3c next: palette-conformance audit — centralize the locked\n16-color palette as named constants in render.","tokensafe/5e2f602":"MCP `solana_token_safety_check` was serving full analysis for free —\nevery call was a lost $0.008. Both MCP tools now serve lite data with a\nstructured `full_report` object (`{url, price_usd, payment_protocol,\nincludes}`) that agents can follow programmatically to the paid REST\nendpoint. The prose string was invisible to code; the object is\nactionable.\n\nSplit `MISSING_REQUIRED_PARAM` from `INVALID_MINT_ADDRESS` so agents\nthat forget `?mint=` get a clear \"you forgot the parameter\" instead of\n\"your address is invalid\" — stops the retry-with-different-addresses\ndeath spiral that burns rate limit budget.\n\nINTEGRATION.md rewritten with real production URL, 402 body format,\nMCP endpoint docs, discovery doc, and complete response schemas for\nboth paid and lite endpoints.","perennials/5e81828":"Two items off the deferred shelf, because their reasons dissolved on a\nsecond look. The shade wash was deferred for want of an hour context in\nthe export — but Share fires from the page that holds the hour, so the\nsheet now exports exactly what it shows: when the wash is drawn, it\nrides into the handed sheet with a footer line naming the hour, the\nseason and the basis. When it isn't, nothing changes.\n\nThe elevation's crowded names take the section drawing's other old\nconvention: staggered text rows. labelRowsFor walks left to right and\nsteps a name crowding its neighbour onto the second row, pure and\nrules-tested, so the screen's elevation and the exported band stagger\nidentically.\n\nCapturing the real Share download to verify also caught a bug older\nthan today: the ground coverage line has always clipped off the\nexported sheet's right edge on any yard with a shaped ground. Footer\nrows now wrap at what 22px actually holds instead of rewording the\nline.","noodles/5eefe5e":"A loop dragged past the last clip loops over silence by design, but the\nplayhead clamped to the content length and sat pinned at the final clip's\nedge until the audio wrapped. The clamp now tops out at the loop end when\na loop is on (it exists to bound runaway extrapolation, not to fence the\nloop), and the arrangement grid sizes itself to cover the loop region so\nthe ruler and lanes exist under the traversal. Verified headless: with\ncontent ending at bar 4 and a loop of 3..7, the playhead walks to 7\nthrough the empty bars and wraps to 3."}